# One environment serves both datasets -- LP and CCPD share src/ and the same
# conda env. There is no second set of requirements.
#
# Pinned below to the versions the published stage-2 run recorded
# (wandb/run-20260419_180409-gloox8hm/files/requirements.txt):
#   Python 3.10 · CUDA 12.2 · single NVIDIA RTX 4090 (24 GB)
#
# A second combination is also known to work, and is what the repository's own
# measurements were taken on (stage-1 throughput, the annotation rebuild, the
# CCPD condition builder):
#   transformers 4.46.3 · peft 0.19.1 · bitsandbytes 0.44.1 · accelerate 1.1.1
#   torch 2.4.0 · CUDA 12.1 · Python 3.10.20
# Everything else matches. bitsandbytes is the one to watch: NF4 behaviour is
# sensitive to its version, so if quantisation misbehaves, try that pair first.
torch==2.4.0
torchvision==0.19.0
diffusers==0.32.2
transformers==4.57.1
peft==0.17.1
accelerate==1.10.1
bitsandbytes==0.48.1
lightning==2.2.4
prodigyopt==1.1.2
safetensors
sentencepiece
protobuf
opencv-python
pillow
numpy
pyyaml
tqdm
wandb

# Evaluation only
lpips
pytorch-fid
lmdb
nltk
natsort
